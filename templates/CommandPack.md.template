# CommandPack：<任务名称>

> 本文件是任务合同，可由项目负责人、规划型 AI、CLI 或插件从 `TASK_INTAKE.md` / `PROJECT_INTAKE.md` 或用户自然语言描述生成。
> 不要求用户直接手写。

## 0. 基本信息

- planId：
- iteration：
- repoRoot：
- base branch：
- 执行模式：FAST / SAFE / AUDIT

## 1. 任务目标

本次只完成：

-

不做：

-

## 2. 必读上下文

默认只需：

- 本 CommandPack
- `skills/codex-ide-executor-zh/SKILL.md`

按需读取：

- `.ai_rules.md`
- `AGENTS.md`
- `docs/CONTEXT_PACK.md`
- `docs/MODULE_BOUNDARY.md`
- `docs/TESTING.md`
- 相关源码和测试

不要默认读取完整规范。

如果任务涉及高风险、不明确或规则冲突，执行代理应 STOP，并要求更明确的 CommandPack 或人工确认。

如果上下文缺失，请 STOP 并报告。

## 3. 允许修改范围

### 用户语义授权

本轮允许处理的功能 / 问题：

-

本轮明确不希望影响的功能 / 业务：

-

### 规划层推断候选路径

以下路径由规划层根据 Task Intake、ContextPack、ModuleBoundary、项目扫描结果推断。

| 路径 | 类型 | 来源 | 可信度 | 说明 |
|---|---|---|---|---|
|  | allow | ContextPack / Repo Scan | high / medium / low |  |
|  | forbid | User Boundary / Risk Rule | high / medium / low |  |

### 执行代理验证要求

- 修改前必须验证候选路径是否存在。
- 如果实际相关文件不在候选允许范围内，必须 STOP。
- 如果需要修改禁止路径，必须 STOP。
- 如果路径可信度为 medium / low，必须先执行只读文件定位阶段。
- 不得凭规划层推断直接扩大范围。

说明：

- 如果需要新增模块，请明确允许对应目录。
- 不要把允许路径压得过窄，导致 AI 被迫把逻辑塞进中央大文件。

## 4. 文件定位阶段

当路径来源为推断、路径可信度不足、或用户只提供语义描述时，执行代理必须先只读定位相关文件。

要求：

- 先搜索关键词、路由、模块名、报错信息；
- 列出 Located Files；
- 标明每个文件为什么相关；
- 判断是否都在允许范围内；
- 如果超出允许范围，STOP；
- 如果涉及高风险模块，STOP；
- 确认范围后再进入修改阶段。

## 5. 合同来源

- Task Intake：
- ContextPack 层级：
- ModuleBoundary：
- 项目扫描：
- 历史 PR / PR_SUMMARIES：
- 生成模型 / 规划者：
- 生成时间：
- 审查状态：draft / reviewed / active

## 上下文选择依据

- 是否使用 `docs/DOCS_MAP.md` 辅助选择上下文：是 / 否
- 选中的文档：
  -
- 未读取的文档及原因：
  -
- 是否存在缺失 / 过期 / 冲突文档：
  -

规则：

- 不默认读取全部 docs。
- 只读取与本任务相关的最小必要文档。
- 如 DOCS_MAP 显示必要文档缺失或过期，必须 STOP 或写入上下文缺口。

## 6. 禁止修改路径

禁止修改：

-

## 7. 具体任务步骤

1.
2.
3.

## 8. UTF-8 / 中文防乱码要求

- 所有文件保持 UTF-8。
- 修改含中文文件必须显式指定编码。
- Python 使用 `encoding="utf-8"`。
- Node 使用 `"utf8"`。
- PowerShell 使用 PowerShell 7 和 `-Encoding utf8`。
- 一旦发现乱码、mojibake 或异常字符，立即 STOP。
- 不得复制乱码上下文继续修改。

## 9. anti-spaghetti 要求

- 不得把新业务逻辑塞进中央大文件。
- 中央入口文件只允许 wiring / 初始化 / 路由接线 / 兼容委托。
- 如需新增模块，应放到合适目录。
- 如果允许路径不足，应 STOP 并报告，不得硬塞。

## 10. 受控顺手修规则

本任务允许执行代理处理低风险、同因、局部、可验证的小问题。

允许：

- 当前任务必要的前置条件小问题；
- 同因同类的小修复；
- import / 类型 / 测试 / 文档小修；
- 不改变业务语义的小脚本问题。

禁止：

- 权限 / license / 计费；
- schema / migration / 用户数据；
- 密钥 / token / 生产配置；
- 依赖升级；
- 跨模块重构；
- 修改禁止路径；
- 业务规则不明确的问题。

所有受控顺手修必须在 ExecutionReport 中列出。

## 11. 必跑门禁

至少运行：

-

如无法运行，说明原因。

## 12. 验收标准

任务完成必须满足：

-

## 13. STOP 条件

遇到以下情况必须停止：

- 上下文不足。
- 指令冲突。
- 允许路径不足。
- 中文乱码。
- 需要修改禁止路径。
- 需要访问密钥。
- 业务规则不清楚。
- 继续执行会制造屎山代码。
- 需要扩大范围。

## 14. 文档回写要求

本次任务完成后，请判断是否需要同步更新项目长期上下文。

需要检查的文档类型：

- 项目说明 / 使用方式文档
- AI 执行规则 / 仓库协作规则
- 项目上下文 / 当前状态文档
- 模块边界 / 架构说明
- 测试 / 构建 / 门禁说明
- 阶段计划 / 路线图
- PR 摘要 / 变更日志
- 文件清单 / 目录索引

本项目对应文档路径：

- 项目说明：{{PROJECT_DOCS}}
- 项目规则：{{RULE_DOCS}}
- 上下文文档：{{CONTEXT_DOCS}}
- 模块边界：{{BOUNDARY_DOCS}}
- 测试门禁：{{TESTING_DOCS}}
- 计划 / 路线图：{{PLAN_DOCS}}
- 变更记录：{{CHANGELOG_DOCS}}
- 文件清单：{{MANIFEST_DOCS}}

如果需要回写但本 CommandPack 未授权修改，请不要擅自扩大范围。
请在 ExecutionReport 中列出"需要文档回写"的文档类型、原因和建议内容。

## 文档地图回写要求

如果本任务新增、删除、重命名重要文档，必须同步检查：

- `MANIFEST.md`
- `docs/DOCS_MAP.md`
- `docs/PR_SUMMARIES.md`

如果当前任务未授权修改这些文件，应在 ExecutionReport 中输出待回写建议。

## 15. ExecutionReport

完成后必须输出：

- 目标。
- 执行模式。
- 修改文件。
- 范围遵守情况。
- 关键实现说明。
- UTF-8 / 中文检查。
- anti-spaghetti 检查。
- 执行的测试 / 门禁。
- 未执行检查及原因。
- 风险与兼容性。
- 回滚方式。
- 规则沉淀建议。
- 下一步建议。

### 受控顺手修记录

- 是否使用受控顺手修：
- 修复内容：
- 为什么属于低风险：
- 验证方式：
- 是否改变业务语义：

## ContextPack 生命周期要求

任务开始前，请判断：

- 当前项目是否已有 ContextPack
- L1 上下文是否足够支撑本次任务
- 是否需要先 Bootstrap L1 ContextPack
- 本次任务完成后是否需要记录 Context Delta

如果本次任务是项目启动、复杂功能、跨模块修改、架构调整、重要 bugfix、发布准备或阶段收口，应要求更新 ContextPack。

如果当前任务不允许修改 ContextPack，请在 ExecutionReport 中输出 Context Delta 建议。

## ExecutionReport 上下文字段

### ContextPack 检查
- 是否已有 ContextPack：
- 本次是否需要 Bootstrap：
- 本次是否产生 Context Delta：
- 是否已更新 ContextPack：
- 如未更新，原因：
- 建议补充内容：

## 文档回写检查
- 是否需要更新 ContextPack：
- 是否需要更新模块边界：
- 是否需要更新测试门禁：
- 是否需要更新 PR_SUMMARIES / CHANGELOG：
- 本次是否已更新：
- 如未更新，原因：

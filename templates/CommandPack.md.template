# CommandPack：<任务名称>

## 0. 基本信息

- planId：
- iteration：
- repoRoot：
- base branch：
- 执行模式：FAST / SAFE / AUDIT

## 1. 任务目标

本次只完成：

-

不做：

-

## 2. 必读上下文

默认只需：

- 本 CommandPack
- `skills/codex-ide-executor-zh/SKILL.md`

按需读取：

- `.ai_rules.md`
- `AGENTS.md`
- `docs/CONTEXT_PACK.md`
- `docs/MODULE_BOUNDARY.md`
- `docs/TESTING.md`
- 相关源码和测试

不要默认读取完整规范。

如果任务涉及高风险、不明确或规则冲突，执行代理应 STOP，并要求更明确的 CommandPack 或人工确认。

如果上下文缺失，请 STOP 并报告。

## 3. 允许修改路径

允许修改：

-

说明：

- 如果需要新增模块，请明确允许对应目录。
- 不要把允许路径压得过窄，导致 AI 被迫把逻辑塞进中央大文件。

## 4. 禁止修改路径

禁止修改：

-

## 5. 具体任务步骤

1.
2.
3.

## 6. UTF-8 / 中文防乱码要求

- 所有文件保持 UTF-8。
- 修改含中文文件必须显式指定编码。
- Python 使用 `encoding="utf-8"`。
- Node 使用 `"utf8"`。
- PowerShell 使用 PowerShell 7 和 `-Encoding utf8`。
- 一旦发现乱码、mojibake 或异常字符，立即 STOP。
- 不得复制乱码上下文继续修改。

## 7. anti-spaghetti 要求

- 不得把新业务逻辑塞进中央大文件。
- 中央入口文件只允许 wiring / 初始化 / 路由接线 / 兼容委托。
- 如需新增模块，应放到合适目录。
- 如果允许路径不足，应 STOP 并报告，不得硬塞。

## 8. 受控灵活性

允许处理同因、局部、小范围、可测的相邻小问题。

不得顺手处理：

- 架构问题。
- 历史技术债。
- 权限 / license / 计费 / schema。
- 生产配置。
- 跨模块重构。
- 需要新增依赖的问题。

## 9. 必跑门禁

至少运行：

-

如无法运行，说明原因。

## 10. 验收标准

任务完成必须满足：

-

## 11. STOP 条件

遇到以下情况必须停止：

- 上下文不足。
- 指令冲突。
- 允许路径不足。
- 中文乱码。
- 需要修改禁止路径。
- 需要访问密钥。
- 业务规则不清楚。
- 继续执行会制造屎山代码。
- 需要扩大范围。

## 12. 文档回写要求

本次任务完成后，请判断是否需要同步更新项目长期上下文。

需要检查的文档类型：

- 项目说明 / 使用方式文档
- AI 执行规则 / 仓库协作规则
- 项目上下文 / 当前状态文档
- 模块边界 / 架构说明
- 测试 / 构建 / 门禁说明
- 阶段计划 / 路线图
- PR 摘要 / 变更日志
- 文件清单 / 目录索引

本项目对应文档路径：

- 项目说明：{{PROJECT_DOCS}}
- 项目规则：{{RULE_DOCS}}
- 上下文文档：{{CONTEXT_DOCS}}
- 模块边界：{{BOUNDARY_DOCS}}
- 测试门禁：{{TESTING_DOCS}}
- 计划 / 路线图：{{PLAN_DOCS}}
- 变更记录：{{CHANGELOG_DOCS}}
- 文件清单：{{MANIFEST_DOCS}}

如果需要回写但本 CommandPack 未授权修改，请不要擅自扩大范围。
请在 ExecutionReport 中列出"需要文档回写"的文档类型、原因和建议内容。

## 13. ExecutionReport

完成后必须输出：

- 目标。
- 执行模式。
- 修改文件。
- 范围遵守情况。
- 关键实现说明。
- UTF-8 / 中文检查。
- anti-spaghetti 检查。
- 执行的测试 / 门禁。
- 未执行检查及原因。
- 风险与兼容性。
- 回滚方式。
- 规则沉淀建议。
- 下一步建议。

## 文档回写检查
- 是否需要更新 ContextPack：
- 是否需要更新模块边界：
- 是否需要更新测试门禁：
- 是否需要更新 PR_SUMMARIES / CHANGELOG：
- 本次是否已更新：
- 如未更新，原因：

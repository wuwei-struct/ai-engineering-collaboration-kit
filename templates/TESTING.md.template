# TESTING

## 1. 普通 PR 必跑

```bash
<lint 命令>
<typecheck 命令>
<test 命令>
```

## 2. 涉及中文文件必跑

```bash
python scripts/check_utf8.py .
```

## 3. 涉及构建 / 发布必跑

```bash
<build 命令>
```

## 4. 涉及导入 / 导出必跑

```bash
<export/import test 命令>
```

## 5. 涉及权限 / license / 计费必跑

```bash
<permission/license test 命令>
```

## 6. 失败处理

门禁失败时必须说明：

- 失败命令。
- 失败摘要。
- 是否与本次改动相关。
- 是否属于历史遗留。
- 是否阻断合并。
- 建议修复路径。

不得在门禁失败时声称全部完成。

## 7. 可接受的未运行情况

-

## 8. 不可接受的隐式跳过

- UTF-8 检查。
- 安全检查。
- 与本次改动直接相关的测试。
- 强制 CI。

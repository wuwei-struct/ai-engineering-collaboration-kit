# Example 03：Web Service Skeleton

这是一个 Web 后端服务的 AI 协作文档骨架示例。

## 项目定位

演示如何为 Web Service 定义 route / controller / service / repository / schema 边界。

## 重点

- routes 只做路由接线。
- controllers 只做请求响应适配。
- services 承载业务规则。
- repositories 承载数据访问。
- schemas 定义输入输出结构。
